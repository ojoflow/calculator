let numbers = Array.from(document.querySelectorAll('.number'));
console.log(numbers);
console.log(numbers[0]);
const add = (x,y) => {
    return x+y
}

const subtract = (x,y) => {
    return x-y;
}

const multiply = (x,y) => {
    return x*y;
}

const divide = (x,y) => {
    return x/y;
}
const operator = (x,y,operand) => {

}